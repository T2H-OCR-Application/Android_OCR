---
phase: 02-firebase
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt, app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt, app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt]
autonomous: true
requirements: [REQ-1.3.2, REQ-1.3.3]
user_setup: []

must_haves:
  truths:
    - "PDF files are uploaded to Cloud Storage before metadata is written to Firestore (D-06)"
    - "Sync starts automatically after a scan is saved (D-03)"
    - "Sync progress is visible via a persistent notification (D-05)"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt"
      provides: "Background sync orchestration"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt"
      to: "androidx.work.WorkManager"
      via: "enqueueUniqueWork"
---

<objective>
Implement the background synchronization pipeline using WorkManager to ensure reliable "File-First" uploads.

Purpose: To fulfill REQ-1.3.2 and REQ-1.3.3, ensuring that scans are backed up to the cloud automatically and resiliently.
Output: A `SyncWorker` that uploads PDFs to Storage and metadata to Firestore, triggered upon scan completion.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/2-CONTEXT.md
@.planning/phases/02-firebase/02-RESEARCH.md
@.planning/phases/02-firebase/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ScanMetadata for Sync State</name>
  <files>app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt</files>
  <action>
    Add `isSynced: Boolean = false` and `remotePdfUrl: String? = null` fields to `ScanMetadata`.
    Ensure the data class is compatible with Firestore (no-arg constructor if needed or @Ignore on paths).
    Add an `updatedAt: Long` field for conflict resolution (D-07).
  </action>
  <verify>
    <automated>grep -E "isSynced|remotePdfUrl|updatedAt" app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt</automated>
  </verify>
  <done>ScanMetadata ready for cloud sync.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SyncWorker</name>
  <files>app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</files>
  <action>
    Create `SyncWorker` extending `CoroutineWorker`.
    Implement the "File-First" sync logic (D-06):
    1. Upload PDF file from local path to `users/{uid}/scans/{scanId}.pdf` in Cloud Storage.
    2. Get download URL.
    3. Update/Create document in Firestore `users/{uid}/scans/{scanId}` with metadata and download URL.
    Use `setForeground()` to show a notification during upload (D-05).
  </action>
  <verify>
    <automated>grep -E "firebase|storage|firestore|putFile" app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</automated>
  </verify>
  <done>SyncWorker implemented with File-First logic and progress notification.</done>
</task>

<task type="auto">
  <name>Task 3: Trigger Sync from ResultsScreen</name>
  <files>app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt</files>
  <action>
    After the local save logic completes in `ResultsScreen`, enqueue a `OneTimeWorkRequest` for `SyncWorker`.
    Use `ExistingWorkPolicy.APPEND` or `REPLACE` based on scan ID to avoid duplicate uploads.
    Ensure `WorkManager` constraints allow any network (D-04).
  </action>
  <verify>
    <automated>grep "enqueue" app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt</automated>
  </verify>
  <done>Sync automatically triggered upon saving a scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Cloud Storage | Large binary data (PDFs) |
| App → Firestore | Structured metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Cloud Storage Upload | mitigate | Use Firebase Security Rules to enforce ownership and file types |
| T-02-04 | Information Disclosure | PDF Content | mitigate | Encrypt transport (TLS) and use per-user isolation (Rules) |
</threat_model>

<verification>
Perform a scan, save it, and verify in the Firebase Console that:
1. The PDF appears in Cloud Storage.
2. The metadata document appears in Firestore with the correct `remotePdfUrl`.
</verification>

<success_criteria>
- WorkManager successfully schedules and executes the upload.
- "File-First" order is maintained (Storage before Firestore).
- Progress notification is visible during the sync.
</success_criteria>

<output>
Create `.planning/phases/02-firebase/02-03-SUMMARY.md` when done
</output>