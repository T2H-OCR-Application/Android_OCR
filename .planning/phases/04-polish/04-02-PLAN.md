---
phase: 04-polish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [app/src/main/java/com/t2h/ocr/domain/ocr/MatExtensions.kt, app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt, app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt, app/src/main/java/com/t2h/ocr/ui/settings/SettingsScreen.kt, app/src/main/java/com/t2h/ocr/data/local/UserPreferences.kt]
autonomous: true
requirements: [D-03, D-04, D-05, NREQ-2.1.1]

must_haves:
  truths:
    - "Native memory (Mat) is released immediately after use"
    - "OCR boundary overlay tracks smoothly (>30fps)"
    - "User can toggle Wi-Fi only sync and clear local cache"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/domain/ocr/MatExtensions.kt"
      provides: "Safe Mat lifecycle management"
    - path: "app/src/main/java/com/t2h/ocr/ui/settings/SettingsScreen.kt"
      provides: "User interface for resource management"
  key_links:
    - from: "DocumentAnalyzer.kt"
      to: "MatExtensions.kt"
      via: "mat.use { ... } blocks"
---

<objective>
Optimize memory management and performance to ensure a smooth user experience and prevent resource exhaustion.

Purpose: Resource hardening and performance polish.
Output: Strict Mat lifecycle management, optimized OCR pipeline, and resource management settings.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
@app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt
@app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Strict Mat Lifecycle</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/MatExtensions.kt, app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt, app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt</files>
  <action>
    Create MatExtensions.kt with an inline 'use' function for Mat objects (per RESEARCH.md pattern 1).
    Refactor DocumentAnalyzer.kt and ImageProcessor.kt to use the 'use' extension for all locally created Mat, MatOfPoint, and MatOfPoint2f instances.
    Ensure that Mat objects returned from functions are clearly documented as 'caller must release'.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>All Mat allocations are wrapped in 'use' or explicitly released.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize OCR Pipeline Latency</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt</files>
  <action>
    Optimize the analysis loop:
    - Use a smaller resolution Mat for boundary detection if current resolution is too high.
    - Reuse Mat headers (gray, blurred, edged) instead of allocating new ones every frame if possible.
    - Profile with androidx.tracing to identify bottlenecks in contour approximation.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Analysis loop latency is minimized, verified by smooth boundary tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Resource Toggles & Settings</name>
  <files>app/src/main/java/com/t2h/ocr/data/local/UserPreferences.kt, app/src/main/java/com/t2h/ocr/ui/settings/SettingsScreen.kt</files>
  <action>
    Create UserPreferences using DataStore to store:
    - sync_wifi_only: Boolean
    - clear_cache_on_sync: Boolean
    Implement SettingsScreen with a Deep Blue themed UI to manage these toggles.
    Add a "Clear Local Cache" button that deletes files from the application's internal files directory.
    Wire the sync_wifi_only setting to the WorkManager constraints in SyncWorker (or where it's enqueued).
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>User can manage sync and storage settings via the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Settings -> App Logic | Settings must be validated and correctly applied to background workers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service | Memory | mitigate | Explicit Mat release prevents OOM crashes |
</threat_model>

<verification>
Monitor memory usage in Android Studio Profiler during intensive scanning. Verify settings persistence.
</verification>

<success_criteria>
No memory leaks detected by LeakCanary. Settings correctly influence app behavior.
</success_criteria>

<output>
Create .planning/phases/04-polish/04-02-SUMMARY.md when done
</output>
