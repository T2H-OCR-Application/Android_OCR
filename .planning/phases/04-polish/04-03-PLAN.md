---
phase: 04-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified: [app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt, app/src/main/java/com/t2h/ocr/ui/results/ResultsViewModel.kt, app/src/main/java/com/t2h/ocr/domain/observability/AnalyticsHelper.kt, app/src/main/java/com/t2h/ocr/ui/components/ErrorState.kt]
autonomous: true
requirements: [D-06, D-07, D-08, NREQ-2.2.1]

must_haves:
  truths:
    - "SyncWorker uses exponential backoff for retries"
    - "Fatal errors (e.g., Camera, Auth) are displayed with high-visibility components"
    - "Custom analytics events track OCR success and sync latency"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/domain/observability/AnalyticsHelper.kt"
      provides: "Unified interface for tracking performance and errors"
    - path: "app/src/main/java/com/t2h/ocr/ui/components/ErrorState.kt"
      provides: "Reusable full-screen error components with recovery actions"
  key_links:
    - from: "SyncWorker.kt"
      to: "AnalyticsHelper.kt"
      via: "Logging success/failure events"
---

<objective>
Enhance the application's resilience to failures and provide comprehensive observability for real-world performance monitoring.

Purpose: Production-grade robustness and monitoring.
Output: Resilient sync pipeline, custom analytics, and high-visibility error handling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
@app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt
@app/src/main/java/com/t2h/ocr/ui/results/ResultsViewModel.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Sync Pipeline & Retry Logic</name>
  <files>app/src/main/java/com/t2h/ocr/ui/results/ResultsViewModel.kt, app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</files>
  <action>
    Update the SyncWorker enqueueing logic in ResultsViewModel.kt to include:
    - setBackoffCriteria(BackoffPolicy.EXPONENTIAL, 10, TimeUnit.SECONDS)
    - Constraints including NetworkType.CONNECTED (or UNMETERED based on D-05 toggles).
    Modify SyncWorker.kt to:
    - Log sync attempt count via AnalyticsHelper.
    - Handle Google account missing errors by returning Result.failure() (manual retry required).
  </action>
  <verify>
    <automated>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.t2h.ocr.SyncWorkerTest</automated>
  </verify>
  <done>Sync pipeline is resilient with exponential backoff and observability.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Observability & Analytics</name>
  <files>app/src/main/java/com/t2h/ocr/domain/observability/AnalyticsHelper.kt, app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt, app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</files>
  <action>
    Create AnalyticsHelper.kt wrapping FirebaseAnalytics.
    Implement tracking for:
    - ocr_performance: latency_ms, page_count, status (D-08).
    - sync_status: scan_id, attempt_count, duration_ms, error_type.
    - user_interaction: screen_view, settings_change.
    Integrate AnalyticsHelper into ScannerViewModel and SyncWorker.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Key performance indicators and user actions are tracked via Analytics.</done>
</task>

<task type="auto">
  <name>Task 3: Create High-Visibility Error Components</name>
  <files>app/src/main/java/com/t2h/ocr/ui/components/ErrorState.kt, app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt, app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt</files>
  <action>
    Implement ErrorState.kt with full-screen Compose components for fatal failures:
    - Camera hardware failure (No permission or hardware error).
    - Authentication lockdown (Account disabled or expired).
    - Network/Sync catastrophic failure (Disk full or unrecoverable API error).
    Each component must include a clear "Recovery Action" (e.g., "Settings", "Retry", "Contact Support") and follow Deep Blue branding.
    Integrate these components into ScannerScreen and ResultsScreen.
  </action>
  <verify>
    <automated>./gradlew recordRoborazziDebug</automated>
  </verify>
  <done>Fatal errors are communicated clearly to the user with actionable steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analytics -> Firebase | Ensure no PII (emails, names) is sent in analytics parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Analytics | mitigate | Sanitize all event parameters to exclude PII |
</threat_model>

<verification>
Simulate network failures and camera permissions denial to verify error screens and retry behavior.
</verification>

<success_criteria>
Errors are handled gracefully, and analytics events are logged correctly (verified via DebugView).
</success_criteria>

<output>
Create .planning/phases/04-polish/04-03-SUMMARY.md when done
</output>
