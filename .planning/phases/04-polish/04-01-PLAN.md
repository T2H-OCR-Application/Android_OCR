---
phase: 04-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, app/build.gradle.kts, app/src/test/java/com/t2h/ocr/domain/ocr/PdfGeneratorTest.kt, app/src/androidTest/java/com/t2h/ocr/sync/SyncWorkerTest.kt, app/src/test/java/com/t2h/ocr/ui/SnapshotTests.kt]
autonomous: true
requirements: [D-01, D-02]

must_haves:
  truths:
    - "Test dependencies are integrated and build passes"
    - "PdfGenerator has >80% coverage including edge cases"
    - "SyncWorker integration tests cover successful upload and failure modes"
    - "Snapshot tests generate golden images for Bento Grid and Scanner UI"
  artifacts:
    - path: "app/src/test/java/com/t2h/ocr/ui/SnapshotTests.kt"
      provides: "Visual regression testing using Roborazzi"
    - path: "app/src/androidTest/java/com/t2h/ocr/sync/SyncWorkerTest.kt"
      provides: "Integration tests for background synchronization"
  key_links:
    - from: "app/build.gradle.kts"
      to: "Roborazzi"
      via: "Plugin and dependency configuration"
---

<objective>
Establish a robust testing suite and quality gates to ensure application stability and prevent regressions.

Purpose: Production hardening through triple-tier testing (Unit, E2E, Visual).
Output: Configured testing framework and expanded test coverage for core components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Testing Infrastructure</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts</files>
  <action>
    Add Roborazzi (1.62.0), MockK (1.14.9), Turbine (1.2.0), and LeakCanary (2.14) to libs.versions.toml and app/build.gradle.kts.
    Apply the Roborazzi plugin and configure the roborazzi extension in app/build.gradle.kts.
    Ensure testOptions are configured for Robolectric (includeAndroidResources = true).
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>Dependencies added and project syncs successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand Logic & Integration Tests</name>
  <files>app/src/test/java/com/t2h/ocr/domain/ocr/PdfGeneratorTest.kt, app/src/androidTest/java/com/t2h/ocr/sync/SyncWorkerTest.kt, app/src/test/java/com/t2h/ocr/data/auth/AuthRepositoryTest.kt</files>
  <behavior>
    - PdfGeneratorTest: Test multi-page wrap, empty text, and special characters.
    - SyncWorkerTest: Verify WorkManager result on successful upload vs. simulated network failure.
    - AuthRepositoryTest: Verify anonymous sign-in and account linking using MockK for FirebaseAuth.
  </behavior>
  <action>
    Implement comprehensive unit tests for PdfGenerator and AuthRepository using MockK.
    Complete the SyncWorkerTest using TestListenableWorkerBuilder to verify the sync pipeline (local to remote).
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest connectedDebugAndroidTest</automated>
  </verify>
  <done>Core logic tests pass with high coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Visual Snapshot Testing</name>
  <files>app/src/test/java/com/t2h/ocr/ui/SnapshotTests.kt</files>
  <action>
    Create a new SnapshotTests class using Roborazzi.
    Capture snapshots for:
    - HomeScreen (Bento Grid)
    - ScannerScreen (with simulated OCR overlays)
    - ResultsScreen (Review UI)
    Use Robolectric and ComposeContentTestRule for capturing snapshots.
  </action>
  <verify>
    <automated>./gradlew recordRoborazziDebug</automated>
  </verify>
  <done>Golden images generated in app/src/test/snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Code -> CI | Tests must not leak credentials or sensitive mock data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Test Logs | mitigate | Ensure no PII or API keys are printed in test logs |
| T-04-SC | Tampering | Testing dependencies | mitigate | Use specific versions and verify checksums via Gradle |
</threat_model>

<verification>
Run the full test suite and confirm visual snapshots are recorded.
</verification>

<success_criteria>
All tests pass, and Roborazzi golden images are established for visual regression tracking.
</success_criteria>

<output>
Create .planning/phases/04-polish/04-01-SUMMARY.md when done
</output>
