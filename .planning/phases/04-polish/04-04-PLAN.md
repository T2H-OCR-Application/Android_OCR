---
phase: 04-polish
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: [app/src/main/res/values/strings.xml, app/src/main/res/values-vi/strings.xml, app/src/main/res/values-es/strings.xml, app/build.gradle.kts, app/proguard-rules.pro]
autonomous: true
requirements: [D-09, D-10, NREQ-2.1.2]

must_haves:
  truths:
    - "All UI strings are extracted and localized (EN/VI/ES)"
    - "R8/ProGuard is enabled for release builds and project builds successfully"
    - "App icon and signing configuration are prepared for release"
  artifacts:
    - path: "app/src/main/res/values-vi/strings.xml"
      provides: "Vietnamese localization"
    - path: "app/src/main/res/values-es/strings.xml"
      provides: "Spanish localization"
    - path: "app/proguard-rules.pro"
      provides: "R8 obfuscation and shrinking rules"
  key_links:
    - from: "app/build.gradle.kts"
      to: "proguard-rules.pro"
      via: "release build type configuration"
---

<objective>
Prepare the application for internationalization and public release by finalizing localization and optimizing the release build.

Purpose: Release readiness and global reach.
Output: Fully localized resources, optimized R8 configuration, and release-ready build.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Localization (EN/VI/ES)</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/res/values-vi/strings.xml, app/src/main/res/values-es/strings.xml</files>
  <action>
    Extract all hardcoded strings from Composable functions across the app into strings.xml.
    Create Vietnamese (values-vi) and Spanish (values-es) versions of strings.xml.
    Ensure that the app_name is updated to "Android OCR" (Deep Blue Brand).
    Audit UI for proper string resource usage (stringResource(id)).
  </action>
  <verify>
    <automated>./gradlew lint</automated>
  </verify>
  <done>App supports EN, VI, and ES with no hardcoded UI strings.</done>
</task>

<task type="auto">
  <name>Task 2: R8 Optimization & Release Config</name>
  <files>app/build.gradle.kts, app/proguard-rules.pro</files>
  <action>
    Enable isMinifyEnabled = true and isShrinkResources = true for the release build type.
    Update proguard-rules.pro with:
    - ML Kit bundled rules (usually automatic, but verify).
    - OpenCV native JNI keep rules.
    - Google Drive API and Firebase serialization keep rules.
    Verify that the release build completes successfully.
  </action>
  <verify>
    <automated>./gradlew assembleRelease</automated>
  </verify>
  <done>Release build is optimized, obfuscated, and significantly smaller in size.</done>
</task>

<task type="auto">
  <name>Task 3: Final Release Prep & Versioning</name>
  <files>app/build.gradle.kts, app/src/main/res/mipmap-*</files>
  <action>
    Update versionCode and versionName in build.gradle.kts to 1 (1.0).
    Verify that a placeholder app icon (Deep Blue style) is present in mipmap folders.
    Configure a signing configuration placeholder (or use debug signing for now but document the path to production signing).
    Final check of the "Bento Grid" layout on different screen sizes (via Preview or emulator).
  </action>
  <verify>
    <automated>./gradlew bundleRelease</automated>
  </verify>
  <done>App is packaged as an AAB/APK ready for Play Store submission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Obfuscated Code -> Attacker | R8 makes reverse engineering significantly harder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Binary | mitigate | R8 obfuscation and resource shrinking |
</threat_model>

<verification>
Perform a final smoke test on a release build. Verify localization by changing device language.
</verification>

<success_criteria>
Release build passes all tests, is localized, and optimized for deployment.
</success_criteria>

<output>
Create .planning/phases/04-polish/04-04-SUMMARY.md when done
</output>
