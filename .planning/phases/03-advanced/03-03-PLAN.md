---
phase: 03-advanced
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: 
  - app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt
  - app/src/main/java/com/t2h/ocr/ui/scanner/CropScreen.kt
  - app/src/main/java/com/t2h/ocr/MainActivity.kt
autonomous: true
requirements: 
  - REQ-1.4.2
must_haves:
  truths:
    - "User sees real-time bounding box overlay matching the document."
    - "User confirms or adjusts crop handles before processing."
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt"
      provides: "Camera Preview with overlay"
    - path: "app/src/main/java/com/t2h/ocr/ui/scanner/CropScreen.kt"
      provides: "Manual crop confirmation UI"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt"
      to: "app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt"
      via: "CameraX ImageAnalysis"
      pattern: "setAnalyzer"
---

<objective>
Implement real-time scanner overlay and manual crop confirmation UI.

Purpose: To fulfill D-03, D-06, and D-07, providing professional-grade scanner feedback and control.
Output: ScannerScreen overlaid with quad points, and a draggable 4-corner CropScreen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced/03-CONTEXT.md
@.planning/phases/03-advanced/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-time Camera Guidance Overlay</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt</files>
  <action>Wire `DocumentAnalyzer` into the CameraX lifecycle in `ScannerScreen`. Collect the Flow of quad coordinates. Use Compose `Canvas` to draw a distinct bounding box overlay over the detected document in real-time (D-06). Map the raw image coordinates to the UI viewport using aspect ratio scaling.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Bounding box draws smoothly over the camera preview.</done>
</task>

<task type="auto">
  <name>Task 2: Manual Crop Confirmation UI</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/CropScreen.kt</files>
  <action>Create `CropScreen.kt` which receives the captured high-res image and the detected quad coordinates. Implement 4 draggable corner handles (D-07 polished interactions with scale transitions). Force the user to tap "Confirm" before proceeding to OCR (D-03).</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>User can drag 4 corners independently to refine the crop region.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Thematic Animations and Haptics</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt, app/src/main/java/com/t2h/ocr/MainActivity.kt</files>
  <action>Implement a camera shutter animation (e.g., a brief flash or fade) and trigger haptic feedback (`HapticFeedbackType.LongPress` or similar via `LocalHapticFeedback`) upon image capture in `ScannerScreen`. Update Compose Navigation in `MainActivity` (or equivalent nav host) to use fluid enter/exit screen transitions between the Scanner, Crop, and Results screens (D-08).</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Shutter animation, haptics, and fluid navigation transitions are functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> ImageProcessor | Crop coordinates passed to native image processing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | CropScreen coordinates | mitigate | Validate coordinate bounds before passing to OpenCV perspective warp to prevent native crashes. |
</threat_model>

<verification>
Coordinate mapping from ImageProxy to Compose Canvas must be mathematically verified.
</verification>

<success_criteria>
Overlay tracks documents in real-time; crop screen allows fine-tuning before OCR.
</success_criteria>

<output>
Create `.planning/phases/03-advanced/03-03-SUMMARY.md` when done
</output>