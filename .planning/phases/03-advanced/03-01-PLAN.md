---
phase: 03-advanced
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - app/build.gradle.kts
  - app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt
  - app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt
autonomous: true
requirements: 
  - REQ-1.1.2
must_haves:
  truths:
    - "App installs successfully without memory crashes"
    - "Documents are automatically outlined on screen"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt"
      provides: "Real-time edge detection via OpenCV"
    - path: "app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt"
      provides: "Perspective warp functions"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt"
      to: "OpenCV Native Memory"
      via: "Release resources"
      pattern: "release\\(\\)"
---

<objective>
Integrate OpenCV to build the custom CameraX analyzer for 4-corner edge detection.

Purpose: Essential for D-06 (Real-time Camera Guidance) and D-03 (Manual Crop) without relying on ML Kit's closed UI intent.
Output: Background analysis flow that outputs quad coordinates, and an ImageProcessor for perspective warping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced/03-CONTEXT.md
@.planning/phases/03-advanced/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate OpenCV Dependency</name>
  <files>app/build.gradle.kts</files>
  <action>Add the `com.quickbirdstudios:opencv` dependency to `app/build.gradle.kts`. Ensure it syncs correctly without memory leak warnings. This is critical for 4-corner document detection as per D-06.</action>
  <verify>
    <automated>./gradlew app:assembleDebug</automated>
  </verify>
  <done>Gradle build succeeds with the OpenCV library included.</done>
</task>

<task type="auto">
  <name>Task 2: Build DocumentAnalyzer</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/DocumentAnalyzer.kt</files>
  <action>Create `DocumentAnalyzer` implementing `ImageAnalysis.Analyzer`. Convert `ImageProxy` to OpenCV `Mat`. Use `Imgproc.findContours` and `Imgproc.approxPolyDP` to locate the largest 4-point quadrilateral (the document). Emit the 4 corners via a Kotlin Flow or Callback. Always release `Mat` objects in a finally block to prevent OOM errors.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Analyzer logic is implemented and safely handles Mat resources.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ImageProcessor for Perspective Warp</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/ImageProcessor.kt</files>
  <action>Create `ImageProcessor.kt`. Add a function to take a high-res image `Mat` and the user-confirmed 4 crop coordinates, then use OpenCV's `getPerspectiveTransform` and `warpPerspective` to flatten and crop the image into a top-down view for ML Kit text extraction.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>ImageProcessor provides a reliable perspective correction method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Camera Input -> Memory | Native memory allocation for OpenCV |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | DocumentAnalyzer | mitigate | Strictly call `.release()` on all OpenCV Mat instances to avoid JVM OOM crashes. |
| T-03-SC | Tampering | Maven installs | accept | No slopcheck for Android Maven. Verified Quickbird studios via Maven Central. |
</threat_model>

<verification>
Review the code for proper `Mat` release closures to ensure stability.
</verification>

<success_criteria>
App builds successfully, analyzer processes frames, and perspective warp function is mathematically complete.
</success_criteria>

<output>
Create `.planning/phases/03-advanced/03-01-SUMMARY.md` when done
</output>