---
phase: 03-advanced
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: 
  - app/src/androidTest/java/com/t2h/ocr/ui/home/HomeScreenTest.kt
  - app/src/main/java/com/t2h/ocr/data/local/JsonStorage.kt
  - app/src/main/java/com/t2h/ocr/ui/home/HomeViewModel.kt
  - app/src/main/java/com/t2h/ocr/ui/home/HomeScreen.kt
autonomous: true
requirements: 
  - REQ-1.4.1
must_haves:
  truths:
    - "User can see a dynamic grid layout of past scans."
    - "User can search scans by title and extracted text."
  artifacts:
    - path: "app/src/androidTest/java/com/t2h/ocr/ui/home/HomeScreenTest.kt"
      provides: "UI verification for Bento Grid and Search"
    - path: "app/src/main/java/com/t2h/ocr/ui/home/HomeScreen.kt"
      provides: "Bento Grid UI"
    - path: "app/src/main/java/com/t2h/ocr/ui/home/HomeViewModel.kt"
      provides: "Search filtering logic"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/ui/home/HomeScreen.kt"
      to: "app/src/main/java/com/t2h/ocr/ui/home/HomeViewModel.kt"
      via: "State Flow collection"
      pattern: "collectAsState"
---

<objective>
Develop the "Bento Grid" home screen and implement deep search, including instrumentation tests for UI verification.

Purpose: To fulfill REQ-1.4.1 and D-04/D-05, replacing a basic list with a polished, dynamic entry point for document management.
Output: A new HomeScreen composable with LazyVerticalGrid, search bar, and associated UI tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced/03-CONTEXT.md
@.planning/phases/03-advanced/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HomeScreen Instrumentation Test Scaffold</name>
  <files>app/src/androidTest/java/com/t2h/ocr/ui/home/HomeScreenTest.kt</files>
  <action>Create `HomeScreenTest.kt` in the instrumentation test directory. Define tests for checking the visibility of the Search Bar and the presence of the Bento Grid items. Use `composeTestRule` for node finding. This serves as the Wave 0 contract for REQ-1.4.1 verification as per the nyquist_compliance requirement.</action>
  <verify>
    <automated>./gradlew connectedAndroidTest</automated>
  </verify>
  <done>Instrumentation test file exists and executes successfully (even if tests are empty/minimal initially).</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deep Search Logic</name>
  <files>app/src/main/java/com/t2h/ocr/data/local/JsonStorage.kt, app/src/main/java/com/t2h/ocr/ui/home/HomeViewModel.kt</files>
  <action>Update `JsonStorage` or the Repository to load scan metadata into memory. In `HomeViewModel`, expose a `StateFlow<List<ScanMetadata>>` that filters based on a search query. The filter must check BOTH `scan.title` and `scan.extractedText` (case-insensitive) to fulfill D-05.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>ViewModel exposes correctly filtered results based on search query state.</done>
</task>

<task type="auto">
  <name>Task 3: Build Bento Grid UI</name>
  <files>app/src/main/java/com/t2h/ocr/ui/home/HomeScreen.kt</files>
  <action>Create `HomeScreen.kt`. Use Jetpack Compose `LazyVerticalGrid(columns = GridCells.Fixed(2))`. Implement span logic: the newest item (index 0) spans 2 columns, while older items span 1 column (D-04). Add a Search Bar component at the top that updates the ViewModel's query. Ensure Deep Blue brand colors are used (D-10).</action>
  <verify>
    <automated>./gradlew connectedAndroidTest</automated>
  </verify>
  <done>Bento grid displays items correctly with variable span logic and search input, verified by instrumentation tests using connectedAndroidTest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Input -> Search Query | Local search filtering |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | HomeScreen Search | mitigate | Ensure search only queries the current user's local JSON metadata. |
</threat_model>

<verification>
Check UI preview for Bento Grid span logic visually if possible, or verify unit tests for search filtering and instrumentation tests for UI state.
</verification>

<success_criteria>
Home screen displays a visually appealing bento grid that filters instantly based on text/title search, with full UI verification via instrumentation tests.
</success_criteria>

<output>
Create `.planning/phases/03-advanced/03-02-SUMMARY.md` when done
</output>
