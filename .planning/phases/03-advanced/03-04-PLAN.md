---
phase: 03-advanced
plan: 04
type: execute
wave: 3
depends_on:
  - "03-03"
files_modified: 
  - app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt
  - app/src/main/java/com/t2h/ocr/ui/scanner/GalleryScreen.kt
  - app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt
  - app/src/test/java/com/t2h/ocr/domain/ocr/PdfGeneratorTest.kt
autonomous: true
requirements: 
  - REQ-1.2.2
  - REQ-1.2.3
must_haves:
  truths:
    - "User can scan multiple pages in one session."
    - "Generated PDF uses standard text rendering and pagination, not images."
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt"
      provides: "StaticLayout multi-page PDF generation"
    - path: "app/src/main/java/com/t2h/ocr/ui/scanner/GalleryScreen.kt"
      provides: "Batch scan review"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt"
      to: "app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt"
      via: "PDF Generation Trigger"
      pattern: "PdfGenerator"
---

<objective>
Enable multi-page batch scanning and rewrite PDF generation for clean text.

Purpose: To fulfill D-01 (Batch Flow) and D-02 (Clean Text Document), solving the Phase 1 PDF rendering issue.
Output: Gallery review screen, state management for multiple pages, and updated PdfGenerator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-advanced/03-CONTEXT.md
@.planning/phases/03-advanced/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite PdfGenerator (TDD)</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt, app/src/test/java/com/t2h/ocr/domain/ocr/PdfGeneratorTest.kt</files>
  <behavior>
    - Test 1: Given short text, generates 1-page PDF.
    - Test 2: Given long text exceeding page height, generates multi-page PDF.
  </behavior>
  <action>Write tests first. Then rewrite `PdfGenerator` to abandon image wrapping. Use `android.text.StaticLayout` combined with `canvas.translate()` to render the concatenated OCR text from all scanned pages onto clean white `PdfDocument` pages (D-02). Handle pagination when text exceeds page bounds.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.t2h.ocr.domain.ocr.PdfGeneratorTest"</automated>
  </verify>
  <done>PdfGenerator creates clean, multi-page text PDFs based on test criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Batch Scan State Management</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt</files>
  <action>Update `ScannerViewModel` to hold a list of `ScannedPage` items instead of a single page. After a crop is confirmed (from CropScreen), append the OCR result to this list and return the user to the ScannerScreen to scan the next page (D-01).</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>ViewModel manages multiple pages successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Gallery Review Screen</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/GalleryScreen.kt</files>
  <action>Create `GalleryScreen.kt` to allow users to review the batch of scanned pages before finalizing. Provide a "Done" button that triggers the multi-page PDF generation via the `PdfGenerator`. Incorporate a shimmer loading state (D-09) while processing.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <done>Users can review batched pages and generate the final PDF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System -> PDF Generation | Writing PDF files to local storage |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | PdfGenerator | mitigate | Impose reasonable text length/page limits to prevent massive memory allocations during PDF creation. |
</threat_model>

<verification>
Generate a large multi-page document to verify pagination logic in PdfGenerator.
</verification>

<success_criteria>
App successfully strings together multiple camera captures into a single, clean, text-based PDF document.
</success_criteria>

<output>
Create `.planning/phases/03-advanced/03-04-SUMMARY.md` when done
</output>