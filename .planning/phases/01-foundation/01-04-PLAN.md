---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [app/src/androidTest/java/com/t2h/ocr/ScanFlowTest.kt, app/src/main/java/com/t2h/ocr/ui/theme/Theme.kt]
autonomous: true
requirements: [REQ-1.4.4, NREQ-2.1.1, NREQ-2.1.2]
must_haves:
  truths:
    - "Application remains stable during multiple consecutive scans"
    - "UI correctly toggles between Dark and Light mode while preserving branding"
    - "No ImageProxy leaks observed (Scanner does not freeze after multiple frames)"
  artifacts:
    - path: "app/src/androidTest/java/com/t2h/ocr/ScanFlowTest.kt"
      provides: "Automated end-to-end smoke test"
---

<objective>
Conduct final verification of the Phase 1 MVP, focusing on UX polish, theme consistency, and basic automated testing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Smoke Test</name>
  <files>app/src/androidTest/java/com/t2h/ocr/ScanFlowTest.kt</files>
  <action>
    Create a basic Compose UI test that:
    - Verifies the Permission Rationale screen appears on fresh install.
    - Navigates to the Scanner Screen.
    - (Mock or manual trigger) navigates to Results Screen and checks text display.
  </action>
  <verify>
    <automated>./gradlew connectedAndroidTest</automated>
  </verify>
  <done>Basic smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: UX Polish & Theme Consistency</name>
  <files>app/src/main/java/com/t2h/ocr/ui/theme/Theme.kt</files>
  <action>
    Review the app's look in both Dark and Light mode.
    - Ensure Deep Blue remains the dominant brand color.
    - Check text legibility and accessibility (contrast).
    - Add basic transition animations between screens in `MainActivity`.
  </action>
  <verify>
    App looks good and functions correctly in both system themes.
  </verify>
  <done>UI is polished and theme-compliant.</done>
</task>

<task type="auto">
  <name>Task 3: Performance & Leak Audit</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt</files>
  <action>
    Perform a manual "stress test":
    - Open scanner and let it run for 1 minute.
    - Perform 5 back-to-back scans.
    - Monitor logcat for any ML Kit errors or memory warnings.
    - Confirm `ImageProxy.close()` is hit for every frame.
  </action>
  <verify>
    Scanner remains responsive and memory usage is stable.
  </verify>
  <done>Performance check completed, no leaks found.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | Image Analysis | mitigate | Ensure strict ImageProxy closing to prevent frame drops |
</threat_model>

<success_criteria>
Phase 1 MVP is stable, verified, and ready for Phase 2.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-04-SUMMARY.md when done
</output>
