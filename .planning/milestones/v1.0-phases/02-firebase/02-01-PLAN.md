---
phase: 02-firebase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/src/androidTest/java/com/t2h/ocr/SyncWorkerTest.kt, gradle/libs.versions.toml, app/build.gradle.kts, app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt, app/src/main/java/com/t2h/ocr/MainActivity.kt]
autonomous: true
requirements: [REQ-1.3.1, REQ-1.3.2, REQ-1.3.3]
user_setup: []

must_haves:
  truths:
    - "Test infrastructure for SyncWorker exists"
    - "Firebase Auth initializes silently on app launch"
    - "User is automatically signed in anonymously if not already authenticated"
    - "Auth state is accessible throughout the app"
  artifacts:
    - path: "app/src/androidTest/java/com/t2h/ocr/SyncWorkerTest.kt"
      provides: "SyncWorker test coverage"
    - path: "app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt"
      provides: "Authentication management logic"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/MainActivity.kt"
      to: "com.google.firebase.auth.FirebaseAuth"
      via: "AuthRepository initialization"
---

<objective>
Initialize test infrastructure, setup Firebase dependencies, and establish the anonymous identity foundation.

Purpose: To ensure every user has a unique ID (UID) from the first launch, enabling seamless data association, and to prepare the validation infrastructure for background sync.
Output: Integrated Firebase Auth dependencies, a functioning anonymous authentication flow, and test scaffolding.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/2-CONTEXT.md
@.planning/phases/02-firebase/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create SyncWorkerTest</name>
  <files>app/src/androidTest/java/com/t2h/ocr/SyncWorkerTest.kt</files>
  <action>
    Create the `SyncWorkerTest` instrumented test class in the `androidTest` directory.
    Add placeholders/skeleton tests for verifying successful file upload logic and metadata sync to Firestore logic as identified in research.
  </action>
  <verify>
    <automated>ls app/src/androidTest/java/com/t2h/ocr/SyncWorkerTest.kt</automated>
  </verify>
  <done>SyncWorkerTest class exists and builds.</done>
</task>

<task type="auto">
  <name>Task 1: Add Firebase & WorkManager Dependencies</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts</files>
  <action>
    Add the following to `gradle/libs.versions.toml`:
    - `androidx-work-runtime-ktx = { group = "androidx.work", name = "work-runtime-ktx", version = "2.11.2" }`
    - `play-services-auth = { group = "com.google.android.gms", name = "play-services-auth", version = "21.3.0" }`
    
    Add them to `app/build.gradle.kts` dependencies section. 
    Note: Firebase dependencies are already present via BOM, but ensure `firebase-auth` is there.
  </action>
  <verify>
    <automated>grep -E "work-runtime-ktx|play-services-auth" app/build.gradle.kts || grep -E "work-runtime-ktx|play-services-auth" gradle/libs.versions.toml</automated>
  </verify>
  <done>Dependencies resolved and project syncs successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement AuthRepository</name>
  <files>app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt</files>
  <action>
    Create `AuthRepository` class to manage Firebase Auth operations.
    Implement `signInAnonymously()` using `FirebaseAuth.getInstance().signInAnonymously()`.
    Expose a `currentUser` Flow or State to track authentication status.
    Per D-01: Ensure it handles already-signed-in users gracefully.
  </action>
  <verify>
    <automated>grep "signInAnonymously" app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt</automated>
  </verify>
  <done>AuthRepository created with anonymous sign-in logic.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Anonymous Auth in MainActivity</name>
  <files>app/src/main/java/com/t2h/ocr/MainActivity.kt</files>
  <action>
    In `onCreate`, instantiate `AuthRepository` (or a ViewModel using it) and call `signInAnonymously()` if `currentUser` is null.
    This fulfills D-01 (Anonymous/Deferred Auth) by ensuring identity is established immediately without user interaction.
  </action>
  <verify>
    <automated>grep "signInAnonymously" app/src/main/java/com/t2h/ocr/MainActivity.kt</automated>
  </verify>
  <done>App signs in anonymously on launch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Firebase Auth | Sensitive tokens and credentials cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Auth Session | mitigate | Use Firebase Auth's built-in token management and secure storage |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the app launches and signs in anonymously (check Logcat for Firebase Auth success).
</verification>

<success_criteria>
- Project builds with new dependencies.
- Every app launch ensures an authenticated user (UID exists).
- No mandatory login screen appears (D-01).
</success_criteria>

<output>
Create `.planning/phases/02-firebase/02-01-SUMMARY.md` when done
</output>