---
phase: 02-firebase
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [app/src/main/java/com/t2h/ocr/MainActivity.kt, app/src/main/java/com/t2h/ocr/ui/profile/ProfileScreen.kt, app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt]
autonomous: false
requirements: [REQ-1.3.1]
user_setup:
  - service: google-cloud
    why: "Google Sign-In configuration"
    dashboard_config:
      - task: "Configure OAuth consent screen"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"
      - task: "Create OAuth 2.0 Client ID (Android)"
        location: "Google Cloud Console -> APIs & Services -> Credentials"

must_haves:
  truths:
    - "User can navigate to a Profile screen from the scanner"
    - "Profile screen shows current Auth status (Anonymous vs Linked)"
    - "User can link their account to Google (D-02)"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/ui/profile/ProfileScreen.kt"
      provides: "User management UI"
  key_links:
    - from: "app/src/main/java/com/t2h/ocr/ui/profile/ProfileScreen.kt"
      to: "com.t2h.ocr.data.auth.AuthRepository"
      via: "linkWithGoogle call"
---

<objective>
Implement the Profile screen and account linking to allow users to upgrade from anonymous to permanent accounts.

Purpose: To fulfill D-02 (In-App Trigger for login) and REQ-1.3.1 (User authentication), enabling users to persist their data across devices.
Output: A new Profile screen with Google Sign-In integration and account linking logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/2-CONTEXT.md
@.planning/phases/02-firebase/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Account Linking to AuthRepository</name>
  <files>app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt</files>
  <action>
    Add `linkWithGoogle(idToken: String)` method to `AuthRepository`.
    Use `GoogleAuthProvider.getCredential(idToken, null)` and `currentUser.linkWithCredential(credential)`.
    Handle success/failure results.
  </action>
  <verify>
    <automated>grep "linkWithCredential" app/src/main/java/com/t2h/ocr/data/auth/AuthRepository.kt</automated>
  </verify>
  <done>AuthRepository supports account linking.</done>
</task>

<task type="auto">
  <name>Task 2: Create ProfileScreen</name>
  <files>app/src/main/java/com/t2h/ocr/ui/profile/ProfileScreen.kt</files>
  <action>
    Create a Compose `ProfileScreen` that displays:
    - Current user ID (or "Anonymous")
    - A "Link Google Account" button (D-02).
    Implement the Google Sign-In launcher flow to obtain an ID token.
  </action>
  <verify>
    <automated>grep "rememberLauncherForActivityResult" app/src/main/java/com/t2h/ocr/ui/profile/ProfileScreen.kt</automated>
  </verify>
  <done>ProfileScreen UI implemented with Google Sign-In trigger.</done>
</task>

<task type="auto">
  <name>Task 3: Update MainActivity Navigation</name>
  <files>app/src/main/java/com/t2h/ocr/MainActivity.kt</files>
  <action>
    Add `Profile` to the `Screen` sealed class.
    Update `ScannerScreen` (or add an IconButton in MainActivity's Scaffold) to navigate to the `Profile` screen.
    Handle navigation back from Profile to Scanner.
  </action>
  <verify>
    <automated>grep "Profile" app/src/main/java/com/t2h/ocr/MainActivity.kt</automated>
  </verify>
  <done>Profile screen is reachable from the UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Profile Screen and Google account linking flow.</what-built>
  <how-to-verify>
    1. Launch the app.
    2. Navigate to the Profile screen (click the profile icon).
    3. Verify "Anonymous" status is shown.
    4. Click "Link Google Account".
    5. Complete the Google Sign-In flow.
    6. Verify the status updates to show the Google account info.
  </how-to-verify>
  <resume-signal>Account linking verified</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Google Sign-In | OAuth tokens cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | OAuth Tokens | mitigate | Use secure Google Identity Services SDK and don't log tokens |
</threat_model>

<verification>
Ensure a user can start anonymously and upgrade to a Google account without losing their session ID.
</verification>

<success_criteria>
- Navigation to Profile screen works.
- Google Sign-In prompt appears on button click.
- Account linking succeeds and state updates in UI.
</success_criteria>

<output>
Create `.planning/phases/02-firebase/02-02-SUMMARY.md` when done
</output>