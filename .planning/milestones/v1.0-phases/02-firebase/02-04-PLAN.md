---
phase: 02-firebase
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified: [firestore.rules, storage.rules, app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt]
autonomous: true
requirements: [REQ-1.3.4]
user_setup: []

must_haves:
  truths:
    - "Users can only access their own data in Firestore and Storage (D-08)"
    - "Conflicts are resolved by 'Last Writer Wins' using timestamps (D-07)"
  artifacts:
    - path: "firestore.rules"
      provides: "Firestore security constraints"
    - path: "storage.rules"
      provides: "Cloud Storage security constraints"
  key_links:
    - from: "firestore.rules"
      to: "com.google.firebase.auth"
      via: "request.auth.uid"
---

<objective>
Finalize the security architecture and conflict resolution strategy to ensure data isolation and integrity.

Purpose: To fulfill REQ-1.3.4 and D-08 (Per-User Data Isolation), securing the application's cloud backend against unauthorized access.
Output: Production-ready Firebase Security Rules and robust "Last Writer Wins" implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/2-CONTEXT.md
@.planning/phases/02-firebase/02-RESEARCH.md
@.planning/phases/02-firebase/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Firestore Security Rules</name>
  <files>firestore.rules</files>
  <action>
    Define security rules for Firestore:
    - Allow users to read/write documents in `/users/{userId}/scans/{scanId}` ONLY if `request.auth.uid == userId`.
    - Implement the "Last Writer Wins" logic (D-07): for updates, require `request.resource.data.updatedAt > resource.data.updatedAt`.
  </action>
  <verify>
    <automated>cat firestore.rules | grep "request.auth.uid == userId"</automated>
  </verify>
  <done>Firestore rules secured with per-user isolation and LWW logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Storage Security Rules</name>
  <files>storage.rules</files>
  <action>
    Define security rules for Cloud Storage:
    - Allow users to read/write files in `/users/{userId}/scans/{fileName}` ONLY if `request.auth.uid == userId`.
    - Restrict file types to `application/pdf` and `image/jpeg`.
    - Limit max file size (e.g., 20MB).
  </action>
  <verify>
    <automated>cat storage.rules | grep "request.auth.uid == userId"</automated>
  </verify>
  <done>Cloud Storage rules secured with per-user isolation.</done>
</task>

<task type="auto">
  <name>Task 3: Refine LWW in SyncWorker</name>
  <files>app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</files>
  <action>
    Ensure `SyncWorker` always sets/updates the `updatedAt` timestamp using server time (`FieldValue.serverTimestamp()`) or a reliable client-side timestamp before writing to Firestore.
    Implement retry logic for Firestore updates that fail due to Security Rule violations (which indicates a conflict).
  </action>
  <verify>
    <automated>grep "updatedAt" app/src/main/java/com/t2h/ocr/data/sync/SyncWorker.kt</automated>
  </verify>
  <done>SyncWorker correctly participates in the LWW resolution strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Cloud Backend | Unauthorized access attempts cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | Cross-user data access | mitigate | Enforce `request.auth.uid == userId` in Security Rules |
| T-02-06 | Tampering | Metadata Overwrite | mitigate | Use LWW timestamp check in Firestore rules |
</threat_model>

<verification>
Deploy the rules to Firebase Emulator or Production and verify:
1. User A cannot read User B's data.
2. User A cannot write to User B's path.
3. Attempting to update a document with an older `updatedAt` fails.
</verification>

<success_criteria>
- Security rules pass the Firebase Emulator Suite (if available) or manual cross-account tests.
- Data isolation is strictly enforced at the backend level (D-08).
- Conflict resolution behaves as "Last Writer Wins" (D-07).
</success_criteria>

<output>
Create `.planning/phases/02-firebase/02-04-SUMMARY.md` when done
</output>