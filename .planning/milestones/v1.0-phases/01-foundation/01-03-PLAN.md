---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt, app/src/main/java/com/t2h/ocr/data/local/JsonStorage.kt, app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt, app/src/main/java/com/t2h/ocr/MainActivity.kt]
autonomous: true
requirements: [REQ-1.2.1, REQ-1.1.5, REQ-1.3.5]
must_haves:
  truths:
    - "Generated PDF files are searchable (text can be highlighted/copied)"
    - "Scan metadata is persisted to a local JSON file"
    - "User can view and edit recognized text before saving"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt"
      provides: "Searchable PDF creation logic"
    - path: "app/src/main/java/com/t2h/ocr/data/local/JsonStorage.kt"
      provides: "Local metadata persistence"
---

<objective>
Implement searchable PDF generation and local metadata storage, allowing users to review OCR results and save them as documents.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Searchable PDF Generator</name>
  <files>app/src/main/java/com/t2h/ocr/domain/ocr/PdfGenerator.kt</files>
  <action>
    Implement PDF generation per Pattern 2 in 1-RESEARCH.md:
    - Use `PdfDocument` API.
    - Draw the captured image as the base layer.
    - Draw a transparent text layer on top using coordinates from ML Kit text blocks.
    - Save to the provided `OutputStream`.
  </action>
  <verify>
    Code compiles and uses `PdfDocument`.
  </verify>
  <done>PdfGenerator.kt implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Local JSON Storage</name>
  <files>app/src/main/java/com/t2h/ocr/data/local/JsonStorage.kt</files>
  <action>
    Implement local persistence for `ScanMetadata` using `kotlinx.serialization`:
    - Functions to save/load lists of scans to a `scans.json` file in `context.filesDir`.
    - Handle IO exceptions gracefully.
  </action>
  <verify>
    Storage logic handles serialization/deserialization of ScanMetadata.
  </verify>
  <done>JsonStorage.kt implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Results Screen & Integration</name>
  <files>app/src/main/java/com/t2h/ocr/ui/results/ResultsScreen.kt, app/src/main/java/com/t2h/ocr/MainActivity.kt</files>
  <action>
    Create the Results Screen:
    - Display the recognized text in an editable text field.
    - Add a "Save as PDF" button that triggers `PdfGenerator` and `JsonStorage`.
    - Update `MainActivity` to handle navigation between `PermissionRationale`, `ScannerScreen`, and `ResultsScreen`.
  </action>
  <verify>
    User can scan -> edit text -> save -> PDF file is created in storage.
  </verify>
  <done>ResultsScreen implemented and app flow is connected.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | scans.json | mitigate | Validate JSON structure on load |
| T-01-04 | Information Disclosure | Local Storage | mitigate | Store PDFs and JSON in app-private directory (`filesDir`) |
</threat_model>

<success_criteria>
End-to-end flow from scan to searchable PDF creation with local persistence.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-03-SUMMARY.md when done
</output>
