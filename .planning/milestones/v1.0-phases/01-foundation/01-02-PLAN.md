---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt, app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt, app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt]
autonomous: true
requirements: [REQ-1.1.1, REQ-1.1.2, REQ-1.1.4]
must_haves:
  truths:
    - "Camera preview is visible on the ScannerScreen"
    - "OCR detects text and provides visual bounding box feedback in real-time"
    - "Scanner handles lifecycle correctly (stops when backgrounded)"
  artifacts:
    - path: "app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt"
      provides: "CameraX integration with ML Kit analysis"
    - path: "app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt"
      provides: "Common data model for scans"
---

<objective>
Implement the core OCR scanner using CameraX and ML Kit, providing real-time visual feedback for detected text.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/1-CONTEXT.md
@.planning/1-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Scan Models</name>
  <files>app/src/main/java/com/t2h/ocr/data/models/ScanMetadata.kt</files>
  <action>
    Define the `@Serializable` ScanMetadata data class per 1-RESEARCH.md.
    Fields: id, title, timestamp, ocrText, imagePath, pdfPath, language.
  </action>
  <verify>
    Code compiles and uses `@Serializable`.
  </verify>
  <done>ScanMetadata.kt created.</done>
</task>

<task type="auto">
  <name>Task 2: Scanner ViewModel & ML Kit Setup</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerViewModel.kt</files>
  <action>
    Implement ScannerViewModel:
    - Initialize ML Kit `TextRecognizer`.
    - Expose state for detected text blocks and their bounding boxes.
    - Implement a `processImage` function that uses ML Kit to extract text.
    - Use `ImageProxy.close()` correctly per Pitfall 1 in 1-RESEARCH.md.
  </action>
  <verify>
    ViewModel exists and integrates with ML Kit classes.
  </verify>
  <done>ScannerViewModel.kt implemented.</done>
</task>

<task type="auto">
  <name>Task 3: CameraX Scanner Screen</name>
  <files>app/src/main/java/com/t2h/ocr/ui/scanner/ScannerScreen.kt</files>
  <action>
    Implement ScannerScreen using `LifecycleCameraController` per Pattern 1 in 1-RESEARCH.md:
    - Display `PreviewView` inside an `AndroidView`.
    - Attach `ScannerViewModel`'s analyzer to the controller.
    - Overlay a Canvas to draw bounding boxes.
    - IMPORTANT: Use `CoordinateTransform` to map OCR rects to screen coordinates (avoid Coordinate Mismatch pitfall).
  </action>
  <verify>
    Scanner screen displays camera and draws rects when text is present.
  </verify>
  <done>ScannerScreen.kt implemented with real-time feedback.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | Camera | mitigate | Only use Camera permission when the scanner is active |
</threat_model>

<success_criteria>
Real-time camera preview with text detection bounding boxes visible on screen.
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>
